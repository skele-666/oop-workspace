FUNCTION transform(n):
    binary = [];

    while n > 0:
        quotient = n // 2;
        remainder = n - (quotient * 2);
        binary.APPEND(remainder);
        n = quotient;
    
    binary.REVERSE();
    RETURN binary;
    // Could also make it a string and then add each "number" and convert it at the end?
END